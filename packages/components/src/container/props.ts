export const asideProps = defineProps({
  width: {
    type: String,
    default: null,
  },
})

export const containerProps = defineProps({
  direction: {
    type: String,
  },
})

export const footerProps = defineProps({
  height: {
    type: String,
    default: null,
  },
})
export const headerProps = defineProps({
  height: {
    type: String,
    default: null,
  },
})
